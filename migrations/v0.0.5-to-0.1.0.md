# Migrating from 0.0.5 0.1.0

- Open User/Workspace settings and rename all entries that start with `fontAwesome5Autocomplete.` to `fontAwesomeAutocomplete.` (remove `5` between `fontAwesome` and `Autocomplete`)